# 2026-09-07

## Git 仓库迁移（完成）

- origin 由 `yangzongzhuan/RuoYi-Vue3`（官方仓库）切换为 **`https://github.com/MIKU-RI/rese-vue3.git`**
- 迁移前将 2 个未提交改动落为一个 commit：`708f52c`（进货单批次录入 + 批次弹窗仅改日期也可提交）
- 推送成功，远端 `refs/heads/master = 708f52c`，完整历史 439 commits 已上云

## 排障记录（重要教训）

- 症状：推送稳定报 `remote unpack failed: index-pack failed` / `did not receive expected object 9e50fbff...`，与 Git 版本（WorkBuddy 魔改版 / 系统版 2.55）、代理（127.0.0.1:2129）/直连、--no-thin、repack 均无关
- 根因：**本仓库原为 shallow clone（`--depth 1`，仅 24 commits）**，`.git/shallow` 边界提交 `838965c` 的父提交 `9e50fbff` 不在本地，推到空仓库必然失败
- 解决：`git fetch --unshallow https://github.com/yangzongzhuan/RuoYi-Vue3.git master`（走代理，Gitee 镜像 `y_project/RuoYi-Vue3` 已 404）补全历史后推送成功
- 排查入口：`git cat-file -t <缺失sha>`、`cat .git/shallow`、`git rev-list --count`

## 凭据与环境

- Windows 凭据管理器存有 GitHub `MIKU-RI` 的 PAT（repo/gist/workflow 权限），读取器 `git-credential-wincred.exe`
- WorkBuddy 自带的 `helper-selector` 弹窗不可靠（列表无 MIKU-RI、间歇返回空），已在**本仓库 local 配置** `credential.helper=""` + `credential.helper=wincred` 绕开弹窗
- 系统标准 Git：`C:/Program Files/Git/cmd/git.exe`（2.55.0），可用于交叉验证
- 网络：环境代理 `127.0.0.1:2129` 对 GitHub 请求稳定可用；直连 github.com 时通时断（GFW）

## RuoYi-Vue 后端仓库推送 demo-one（完成）

- 目录 `G:\张晓东\WorkSpace\WorkBuddy\RuoYi-Vue`（Java 后端），原有远程 `miku = MyWorkspace`（保留未动），新增 `origin = https://github.com/MIKU-RI/demo-one.git`（私有空仓库）
- 推送前谱系核查：`prod`(20条业务提交)/`main`(2条)/`appmod`(3条) 三分支均自包含，根为 GitHub Initial commit `1669c52`；仓库里 `.git/shallow` 边界 `13db1fc`（RuoYi 官方提交）与三个分支**无关**，属历史遗留
- 顺手用 `git fetch --unshallow https://github.com/yangzongzhuan/RuoYi-Vue.git`（走代理）把官方完整历史补入对象库并清掉 shallow 标记（无害且防以后踩坑）
- 推送配方同前端仓库：系统版 Git + 代理 + wincred 凭据 + `--no-thin` + HTTP/1.1 + 大 postBuffer，一次成功
- 远端验证：`prod=2fb121b`、`main=d84002e`、`appmod=d015e14`；本仓库同样已固化 local `credential.helper=""` + `wincred`
- 注意：demo-one 的 GitHub 默认分支是 main（README），业务主分支是 prod，未擅自修改默认分支
